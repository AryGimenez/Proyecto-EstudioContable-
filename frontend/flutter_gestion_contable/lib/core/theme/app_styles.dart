// lib/core/theme/app_styles.dart

import 'package:flutter/material.dart'; // Importa la librería principal de Flutter para usar widgets de Material Design

const verticalSpaceSmall = SizedBox(
    height:
        8.0); // Define un espacio vertical pequeño, usando un widget SizedBox con una altura de 8 píxeles
const verticalSpaceMedium = SizedBox(
    height:
        16.0); // Define un espacio vertical mediano, usando un widget SizedBox con una altura de 16 píxeles
const verticalSpaceLarge = SizedBox(
    height:
        32.0); // Define un espacio vertical grande, usando un widget SizedBox con una altura de 32 píxeles

const formPadding = EdgeInsets.symmetric(
    horizontal:
        16.0); // Define un padding (relleno) para los formularios, usando EdgeInsets.symmetric para aplicar un margen horizontal de 16 píxeles
